# 项目交接文档

本文档记录近期已经完成的开发工作，方便后续开发者快速了解项目进展。

## 维护要求

- 每次 Git 提交前先更新本文档。
- 按时间倒序记录本次提交修改了什么，并注明对应提交哈希或待提交状态。
- 只记录实际完成并验证过的内容；尚未验证或仍待处理的事项需要明确标注。

## 最近完成的工作

### 2026-08-21

#### 同步两个远端并保留历史成交盈亏归因（当前合并提交待提交）

- `default/main` 已接收本地规范收口提交 `a5804c0`；推送 `origin/main` 时发现远端新增 `f240919` 和合并提交 `1bbab97`，因此以非快进安全合并方式保留双方历史。
- 冲突处理中保留远端新增的 FIFO 买卖配对、成交费用和盈亏原因说明，同时保留本地的投资专家持仓同步、策略监控增强和前后端规范整理。
- `output/` 目录继续完整保留，不纳入合并差异；同步修正规范回归测试，使其明确验证该目录不会被仓库级忽略规则排除。
- 验证结果：后端全量 `pytest` 901 项通过；冲突相关 Python 文件 Ruff 检查与格式检查通过；投资专家前端文件 ESLint 通过，前端 `pnpm build` 通过；`git diff --check` 通过。

#### 收口 8 月 12 日以来前后端代码规范（当前提交）

- 按 `python-backend-code-standards` 复核并整理 8 月 12 日以来涉及的后端 Python 文件，统一导入顺序、格式、类型边界和服务公开接口，补充投资专家响应模型及近期代码规范回归测试。
- 按同一套可维护性原则整理近期前端改动，补齐 ESLint 9 配置与依赖，收紧 API 和页面类型，统一查询键，并拆分路由懒加载、引导守卫、档位标签和告警状态等模块，消除近期文件的 ESLint 错误与警告。
- 调整开发与 Windows 打包脚本的可移植路径处理；本地 `output/` 目录完整保留，未纳入本次差异。
- 验证结果：近期 139 个 Python 文件通过 Ruff 检查与格式检查；近期 58 个前端文件通过 ESLint（0 错误、0 警告）；前端 `pnpm build` 通过；后端全量 `pytest` 900 项通过；`git diff --check` 通过。

#### 合并远端主干并保留历史成交盈亏归因（`1bbab97`）

- 合并 `origin/main` 的 10 个新增提交，接入股票持仓同步、指定年限历史样本与 Hugging Face 回退、训练状态说明、亮色主题修复和自选股策略监控市场条件。
- 自动合并投资专家存储、API、测试与前端页面，保留 `f240919` 新增的 FIFO 买卖配对、成交费用和盈亏原因说明；交接文档冲突按时间顺序保留双方记录。
- 合并后后端全量 `pytest` 882 项通过，投资专家相关文件定向 Ruff 通过，前端 `pnpm build` 通过；构建仅报告既有的大体积 chunk 提示。
- `output/` 下的本地 UI/启动探针保持未跟踪状态，未纳入合并提交或推送。

#### 完善 AI 投资专家历史成交盈亏归因（`f240919`）

- 历史卖出成交按标的、成交时间和 A 股 T+1 约束，对历史买入成交执行 FIFO 配对，补充买入价、卖出价、买卖费用、毛盈亏、扣费后净盈亏和收益率。
- 历史成交页面新增“为什么盈利 / 亏损”和“买入与退出原因”，解释价差、费用、止盈止损或 VWAP 退出依据；历史买入记录继续兼容缺少配对字段的旧数据。
- 已验证后端投资专家专项测试 29 项通过、定向 Ruff 通过、前端 `pnpm build` 通过、`git diff --check` 通过，并在本地运行页面使用真实成交数据完成联调。
- `output/` 下的本地 UI/启动探针保持未跟踪状态，未纳入提交。

#### 增强自选股策略监控的新闻与隔夜美股条件（待提交）

- 监控规则新增“当前自选股”动态作用域，自选股增删后同步更新监控引擎，无需逐条维护标的列表。
- 股票策略监控可叠加隔夜美股风险环境和个股新闻过滤，并可配置增强数据不可用时降级执行或暂停新入场；增强条件只影响买入与进入选股池，不影响卖出和移出通知。
- 新增按需启动的后台市场上下文快照，实时行情评估只读取内存，不在热路径发起网络请求；旧规则默认关闭增强条件，保持历史行为。
- 监控中心补充市场增强条件编辑、规则标签和上下文通知说明；其他入口创建的价格、梯队和快捷策略规则显式保持关闭。
- 移除 ETF 策略测试中写死的内置策略数量，继续逐项验证所有实际加载策略的矩阵后端契约。
- 验证结果：后端相关测试 71 项通过，后端全量 `pytest` 818 项通过；新增文件 Ruff 检查、前端 `pnpm build` 和 `git diff --check` 通过。

#### 调整亮色模式黄色提示色（待提交）

- 亮色模式下将 Tailwind `amber`、`yellow` 的文字、背景和边框统一替换为蓝色，红、绿、紫等其他语义颜色保持原样；黑夜模式继续使用原有黄色与琥珀色。
- 将 `warning` 语义色和专家档位渐变中的黄色端点改为主题自适应颜色，并同步处理自选星标、换手率和评分区间中的硬编码黄色。
- 重启实际使用的 `3011` 前端开发服务，使 Tailwind 配置重新加载；实测亮色警告条为蓝色，黑夜模式恢复黄色。
- 验证结果：前端 `pnpm build` 通过，`git diff --check` 通过，并在 `3011` 服务上完成计算样式验证。

#### 支持从股票持仓同步并由 AI 投资专家继续管理

- AI 投资专家新增“同步我的持仓”预览与确认入口；同步源固定为本地“股票持仓”模块，明确确认后以其覆盖 AI 原持仓并清空旧挂单；可用现金由用户填写，留空按 0 处理，账户总权益按“持仓市值 + 可用现金”重建。
- 新增不可变 `portfolio_sync_events` 审计记录和 payload hash；执行器恢复与状态展示会选择最新同步事件或后续分钟快照，应用重启后仍能从同步持仓继续。
- 同步后以新账户权益重置回撤基线，避免旧模拟账户峰值误触发熔断；重新启动盯盘后，导入持仓即使不在新候选池中也会订阅实时分钟行情并继续执行退出规则。
- 同步仅在盯盘停止且无后台任务时允许，预览会检查空持仓、无效成本/数量、非整手数量和缺失价格；功能只影响模拟盘，不连接券商或发送真实委托。
- 验证结果：88 项 paper-agent、股票持仓及仓库索引测试通过；相关后端文件 Ruff、`main.py` 语法和新 API 路由注册检查通过；前端 `pnpm build` 与差异格式检查通过。

#### 接入 TickFlow 优先、Hugging Face 自动回退的指定年限样本构建（`fec1c92`）

- AI 投资专家页面可选择 1–5 年样本；TickFlow 能覆盖的日期直接使用 TickFlow，TickFlow Pro 三年窗口的较早区间自动从公开 `neigezhu/china-a-share-1min-ohlcv` 归档回填，完全没有 TickFlow 分钟权限时则以归档最新日期为终点向前取指定年限。
- 将候选范围 Hugging Face 回填能力抽入应用数据源模块，后台任务可直接执行，无需人工先跑脚本；保留命令行脚本作为运维入口，并支持按 commit SHA 固定快照和断点续跑。
- 按项目分钟契约把 Hugging Face 成交量从“股”除以 100 转为“手”，价格保持未复权、`turnover` 映射为成交额；只接受上游审计明确确认的分钟缺口，不插值或合成行情。
- 验证结果：51 项投资专家、数据集、训练、API 和仓库定向测试通过；相关新增/修改后端文件 Ruff 检查通过；前端 `pnpm build` 通过；真实 Hugging Face 固定 SHA 单证券/单日临时回填成功（241 根分钟线，未写入项目数据目录）。

#### 修复 AI 投资专家三年样本本地分钟数据回退（`37b5d53`）

- 三年样本优先判断历史分钟远程权限；无三年权限时依次检查股票、指数和 ETF 本地 Parquet 分区，股票分钟库覆盖三年即可启动当前股票候选训练。
- 新增可续传的 Hugging Face 历史分钟回填脚本，按 Point-in-Time 当日候选与前一日候选精确导入，不覆盖既有全市场分钟分区。
- 接入上游逐证券日无分钟数据审计；仅允许已核实的交易日分钟缺口跳过无标签样本，未审计缺口仍会阻断构建，且不生成合成 K 线。
- 已实际构建 2023-08-21 至 2026-08-20 三年样本：727 个交易日、36,350 条候选、11,208,391 条分钟记录；80 个缺口全部命中上游审计，应用数据集状态已更新为成功。
- 验证结果：后端全量 `pytest` 879 项通过，本次新增和修改的数据集/回填文件 Ruff 检查通过。

#### 同步 origin 并整合本地 AI 投资专家改动（待提交）

- 将 `origin/main` 的 4 个新增提交合并到本地 `main`，保留市场研究、历史分钟数据源、策略研究及投资专家绩效扩展。
- 冲突处理中保留本地行业美股因子、组合独立“现价”列、模型运行状态说明，并接入主干新增的持仓市值、浮盈、收益率和历史成交展示。
- 已通过 68 项投资专家与美股概览相关后端测试、冲突文件 Ruff 检查、前端生产构建及差异格式检查。

#### 完善 AI 投资专家训练模型状态说明（`0279664`）

- 训练模型状态区分规则基线、未通过门控、已启用和风险停用，不再把未启用模型描述为“影子观察”。
- 训练模型卡片的详细说明同步展示是否参与模拟决策、是否使用规则基线及训练样本数。
- 已通过 10 项模型训练与存储测试、Ruff 检查、前端生产构建及差异格式检查。

#### 增加 AI 投资专家组合现价并修复标题可见性（`490af1c`）

- “当前组合”新增现价列，读取模拟交易执行器最近一根已完成分钟线的未复权收盘价，并兼容持久化组合快照；没有有效行情时显示 `--`。
- 为“自主模拟交易运行时”和各面板标题补充主题前景色，确保亮色、暗色模式下均清晰可见。
- 已通过 15 项投资专家服务测试、Ruff 检查、前端生产构建及差异格式检查。

#### 按行业重构 AI 投资专家隔夜美股因子（`7128bc0`）

- 将进化实验的样本不足结果由 `shadow` 调整为 `inconclusive`，页面统一显示“未形成结论”，并兼容历史实验状态。
- 将隔夜美股影响从统一大盘因子改为行业/主题传导因子；A 股候选通过行业映射匹配美股 ETF，候选和买入同向加减分，软性卖出反向加减分。
- 美股大盘涨跌只保留为背景信息，不再统一改变候选排序或全局禁止买入；行业数据缺失、过期或无法映射时按中性处理。
- 增加行业 ETF 20 日波动率标准化及缓存，保留止损、最大回撤等硬风控优先级，并在投资专家页面展示行业因子摘要。
- 已通过 52 项相关后端测试、Ruff 检查、前端生产构建、后端主应用导入及差异格式检查。

#### 合并主干并扩展市场看板、AI 投资专家与策略研究（当前提交待提交；功能提交 `20440f2`）

- AI 投资专家新增持仓成本、浮动盈亏、累计盈利、成交单数、胜率和盈亏比，并提供历史成交与当时选股原因、决策特征展示。
- 美股看板补充实时链路、覆盖范围、数据路径、扩展市场宽度和高振幅榜，同时保留主干新增的全市场行业聚合、主题分组和个股浏览能力。
- 新增历史分钟数据源插件、分钟数据完整性校验、板块上下文、量缩突破策略及资金利用率研究/回测脚本和测试。
- 已合并 `origin/main` 至 `1b729c6`，保留主干的新闻情绪、股票持仓、分钟数据持久化和市场日历等更新；合并冲突按双方数据契约逐项处理。
- 验证结果：后端全量 `pytest` 853 项通过；前端 `pnpm build` 通过；本次冲突涉及文件定向 Ruff 通过。全仓 Ruff 仍有主干既有基线问题，未在本次合并中扩大范围处理。
- `output/` 下的本地 UI/启动探针仍保持未跟踪状态，未纳入提交或推送。

#### 补齐新增左侧菜单的显隐设置（待提交）

- 在“设置 → 菜单设置”中补充美股看板、基金账户和 AI 投资专家，三个新增入口现在可通过眼睛图标控制侧栏显示或隐藏。
- 已核对设置页与实际侧栏均包含 19 个内置入口；前端 `pnpm build` 和 `git diff --check` 通过。

### 2026-08-20

#### 建立开发交接文档和提交维护规则（待提交）

- 新增本交接文档，并根据最近 8 个 Git 提交整理近期开发内容。
- 更新根目录 `AGENTS.md`，要求以后每次提交前先记录本次提交修改了什么。

#### 增强 AI 投资专家消息面评分（`9b9b382`）

- 新增新闻情绪分析服务，并接入市场新闻与 AI 投资专家服务。
- 扩展投资专家的模型、运行时状态和前端展示，使消息面评分进入分析结果。
- 增加新闻情绪、投资专家服务及运行时演进相关测试。

#### 完善美股市场与 AI 投资专家（`4f1305a`）

- 新增美股行情数据、参考数据、交易日历、标的服务和行业板块服务。
- 扩展美股 API、市场概览以及 AI 投资专家对美股的支持。
- 前端新增美股标的浏览组件，完善美股仪表盘、投资专家页面、查询键和字段标签。
- 补充自定义数据源文档，以及美股标的、板块、交易日历和投资专家相关测试。

#### 增加监控告警筛选（`fe78f76`、`a751216`）

- 监控页面新增买入、卖出告警筛选。
- 告警 API 和存储层支持按自选股、持仓股筛选，前端同步增加筛选交互。
- 新增告警筛选测试。

#### 修复分钟价格浮点误差（`4042b33`）

- 严格分钟成交判断现在可以容忍合理的浮点噪声。
- 新增对应的严格分钟成交回归测试。

#### 完成本地分钟数据持久化（`fd9fc9c`）

- 统一股票、ETF 和指数分钟数据的路由、存储和状态查询。
- 完善分钟数据同步、仓库、日常流水线以及 TickFlow provider 的衔接。
- 加强 AI 投资专家对重复或重放分钟 K 线的处理，并支持安全恢复中断状态。
- 补充分钟路由、存储状态、provider、投资专家数据集和训练流程相关测试。

#### 修复 Windows 下 Tesseract 检测（`40a1c1b`）

- 自选股 OCR 服务可以识别 Windows 上已经安装的 Tesseract。
- 新增 Windows 检测路径的回归测试。

#### 新增股票持仓管理（`ba6cfdf`）

- 新增股票持仓管理后端 API 与服务。
- 新增持仓页面和持仓编辑弹窗，并接入路由、导航菜单、查询键及前端 API。
- 补充持仓服务、API 和 SPA 回退测试。

## 当前状态

- 上述记录基于 2026-08-20 最近 8 个 Git 提交整理。
- 整理本文档前工作区无未提交修改，最近提交为 `9b9b382`。
